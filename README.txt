# Get_Next_Line
A project done during the Cadet program in 42 Silicon Valley that is a function that reads a line in a text file, returning 1 if the file is done after that line, 0 if the file is done, or a -1 if the file is read incorrectly.

Differences from the PDF:
---
-Put all of the files in another folder called "PUSH_get_next_line"

Project done by:
---
Dylan Tai
09/30/2019